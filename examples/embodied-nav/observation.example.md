# EXAMPLE ONLY — not a verified project observation

## Observation

A hypothetical pilot design would measure whether right-turn waypoint execution is delayed on a fixed route set. No result is asserted here.

## Evidence required

A registered RUN ID, raw route-level metric, config, dataset manifest, and exact commit.

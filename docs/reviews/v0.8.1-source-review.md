# ResearchFlow 0.8.1 Source Review

Date: 2026-09-17
Scope: the local V0.8.1 diff from V0.8.0 in `ResearchOS`
Result: no blocking correctness defect found; suitable for a local checkpoint

## Reviewed behavior

- Method occurrences are role-aware. Prior-work and comparison endpoints no longer create adoption-family matches.
- Exact canonical-method failure propagation remains available; broader-family propagation additionally requires shared Task, Assumption, or FailureCondition context.
- Structural and semantic generator version changes participate in build fingerprints.
- Paper fingerprints, concept resolution, Corpus inputs, and semantic build inputs are reused within one operation where implemented.
- A human benchmark with a negative label that conflicts with an accepted deterministic ontology relation fails closed instead of reporting misleading metrics.
- Existing V1 extraction behavior and the provider-neutral/human-review boundary remain intact.

## Verification

- Full suite: 116 tests passed.
- Focused adjacency tests cover comparison-endpoint exclusion, guarded failure propagation, ontology conflict rejection, concept-ledger reuse, and currentness-input reuse.
- `compileall`, CLI loading, project-state validation, and `git diff --check` passed.
- Real-project application evidence is kept separate: `embodied-nav` benchmark results validate behavior on that reviewed benchmark only, not semantic truth or scientific claims.

## Non-blocking follow-up

- `build()` and `preview()` still repeat some Corpus/concept work.
- Real-project `status` and graph operations remain minute-scale.
- Semantic-v2.1 misses four accepted human-positive benchmark relations and leaves 30 generated candidates outside benchmark coverage.
- These follow-ups require a new generator/versioned fingerprint if candidate semantics change; performance-only changes must preserve output identity.
